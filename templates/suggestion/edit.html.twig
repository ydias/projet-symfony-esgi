{% extends 'base.html.twig' %}

{% block title %}Edit Suggestion{% endblock %}

{% block body %}
    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4">Suggestion</h1>
        </div>
    </div>
    <h1>Edit Suggestion</h1>

    {{ include('suggestion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('suggestion_index') }}">back to list</a>

    {{ include('suggestion/_delete_form.html.twig') }}
{% endblock %}
